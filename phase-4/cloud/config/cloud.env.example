# Cloud Agent Configuration
# Phase 4 - Platinum Tier
# Agent Role: CLOUD (drafts, triage, scheduling only)
# IMPORTANT: This file contains NO credentials - URLs and configuration only

# Agent Identity
AGENT_ROLE=CLOUD
AGENT_NAME=cloud
VAULT_PATH=/vault

# Watcher Configuration
WATCHER_INTERVAL=30
GMAIL_WATCHER_ENABLED=true
FILESYSTEM_WATCHER_ENABLED=true

# Allowed Actions (Cloud cannot send/post/pay)
ALLOWED_ACTIONS=triage,draft,schedule,monitor

# Odoo Configuration (Cloud - draft only)
ODOO_URL=http://localhost:8069
ODOO_DB=odoo
ODOO_USER=admin
# ODOO_PASSWORD set in local environment only
ODOO_MODE=draft

# MCP Servers (Cloud - draft operations only)
MCPS_ENABLED=mcp_odoo_draft
MCP_ODOO_DRAFT_ONLY=true

# Sync Configuration
SYNC_INTERVAL=30
GIT_REPO_PATH=/vault
GIT_BRANCH=main
GIT_REMOTE=origin

# Health Server
HEALTH_PORT=8080
HEALTH_RATE_LIMIT=1
HEALTH_CHECK_INTERVAL=60

# Logging
LOG_LEVEL=INFO
LOG_FILE=/var/log/ai-employee/agent.log

# Security
SECRET_CHECK_ENABLED=true
BLOCK_MODIFIES_DASHBOARD_MD=true
